# Project-specific config (contains paths, team names, webhooks)
project.env

# Runtime state (logs, metrics, queue, outputs)
data/
outputs/
*.log

# Claude Code session state (worktrees, settings)
.claude/

# macOS
.DS_Store

# Credentials
credentials.toml
*.key
*.pem
